<%= form_with(model: shoot_location) do |form| %>
  <% if shoot_location.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(shoot_location.errors.count, "error") %> prohibited this shoot_location from being saved:</h2>

      <ul>
        <% shoot_location.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :series_id, style: "display: block" %>
    <%= form.text_field :series_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
